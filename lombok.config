# Let JaCoCo skip Lombok-generated code in coverage reports
lombok.addLombokGeneratedAnnotation = true

# Warn when @EqualsAndHashCode/@ToString omits super class fields
lombok.equalsAndHashCode.callSuper = warn
lombok.toString.callSuper = warn

# Use standard SLF4J logger field name
lombok.log.fieldName = log
