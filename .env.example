# Copy to .env and fill in real values. .env is gitignored — never commit it.

# Infrastructure (docker-compose.yml)
POSTGRES_USER=
POSTGRES_PASSWORD=
RABBITMQ_USER=
RABBITMQ_PASS=

# Homelab deployment (docker-compose.homelab.yml)
HOMELAB_IP=
