# Copy to .env and fill in real values. .env is gitignored — never commit it.
# All variables have built-in defaults in docker-compose.yml — .env is optional.

# Infrastructure
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
RABBITMQ_USER=guest
RABBITMQ_PASS=guest
