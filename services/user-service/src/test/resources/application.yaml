spring:
  cloud:
    config:
      enabled: false
    loadbalancer:
      enabled: false
    discovery:
      enabled: false

eureka:
  client:
    enabled: false

keycloak:
  admin:
    server-url: http://localhost:18080
    realm: test-realm
    client-id: test-client
    client-secret: test-secret
