# Test configuration for Incident Service
# Disable Config Server import for tests
spring:
  config:
    import: "optional:configserver:"

# Disable Eureka client for tests
eureka:
  client:
    enabled: false

# Disable RabbitMQ for tests
  rabbitmq:
    host: localhost
    port: 5672