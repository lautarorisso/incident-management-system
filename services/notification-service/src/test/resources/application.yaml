# Test configuration for Notification Service
# Disable Config Server import for tests
spring:
  config:
    import: "optional:configserver:"
  rabbitmq:
    host: localhost
    port: 5672

# Disable Eureka client for tests
eureka:
  client:
    enabled: false