package com.lautarorisso.notification_service.enums;

/**
 * Delivery state of a notification (independent of read state).
 */
public enum NotificationDeliveryStatus {
    PENDING,
    SENT,
    FAILED
}
